# Air configuration for local development
root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/identity ./main.go"
bin = "./tmp/identity"
full_bin = "./tmp/identity"
include_ext = ["go", "mod", "sum", "yaml"]
exclude_dir = ["tmp", "vendor"]

[log]
time = true

[misc]
clean_on_exit = true
